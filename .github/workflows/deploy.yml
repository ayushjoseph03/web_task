name: CI-CD Pipeline

on:
  push:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest

    steps:

      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Run Security Scan
        run: |
          echo "Running security check..."

          if grep -r "<script>" .; then
            echo "Warning: Script tag found!"
          else
            echo "No risky scripts found."
          fi

      - name: Validate index1.html Exists
        run: |
          if [ -f index1.html ]; then
            echo "index1.html found"
          else
            echo "index1.html missing"
            exit 1
          fi

      - name: Build Complete
        run: |
          echo "Pipeline executed successfully"

      - name: Alert on Failure
        if: failure()
        run: |
          echo "ALERT: Pipeline failed"